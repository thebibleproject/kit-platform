extend schema
    @link(
        url: "https://specs.apollo.dev/federation/v2.3"
        import: ["@key", "@interfaceObject"]
    )

type Query {
    tests: [Test!]!
}

type Test {
    id: ID!
    name: String!
}